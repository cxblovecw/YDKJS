// 对象
let obj={
  name:'marco'
}
// 属性访问方式
console.log(obj.name)

let obj2=new Object();
obj2['name']='polo'
// 键访问
console.log(obj2['name'])

// 属性名类型为字符串 若传入的类型不是字符串 则会被转化成字符串

// 数组
var arr=['110','120',119]
arr['type']="phone";
arr[3]='10086';
console.log(arr.length)